import React from 'react';
import PageShell from './PageShell';

const Cart: React.FC = () => (
  <PageShell title="Panier" subtitle="Selection finale avant commande.">
    <p>Revoyez les articles et ajustez les quantites.</p>
  </PageShell>
);

export default Cart;
