import React from 'react';
import PageShell from './PageShell';

const Profile: React.FC = () => (
  <PageShell title="Profil" subtitle="Parametres du compte.">
    <p>Mettre a jour vos informations et preferences.</p>
  </PageShell>
);

export default Profile;
