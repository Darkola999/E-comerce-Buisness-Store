import React from 'react';
import PageShell from './PageShell';

const Users: React.FC = () => (
  <PageShell title="Utilisateurs" subtitle="Gestion des acces et roles.">
    <p>Ajoutez des comptes et configurez les permissions.</p>
  </PageShell>
);

export default Users;
